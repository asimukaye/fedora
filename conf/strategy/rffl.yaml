defaults:
  - 'strategy_schema'
  - cfg: rffl
  - _self_

name: rffl
cfg:
  train_fraction: 1.0
  eval_fraction: 1.0
  delta_normalize: true
  sparsify_gradients: true
  num_clients: ${simulator.num_clients}
  beta: 1.0
  alpha: 0.95
  gamma: 0.15

